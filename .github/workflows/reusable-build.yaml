name: Build Image Reusable

on:
  workflow_call:
    inputs:
      image:
        required: true
        type: string
      version:
        required: true
        type: string
      push:
        required: false
        type: boolean
        default: false
      runs-on:
        required: false
        type: string
        default: container-factory-runner

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  build-single:
    name: Build ${{ inputs.image }}:${{ inputs.version }}
    runs-on: ${{ inputs.runs-on }}
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
      - name: Setup Nix Cache Profile
        uses: gillouche/homelab-ci/actions/setup-aws-profile@main
        with:
          profile: "nix"
          access-key-id: ${{ secrets.NIX_CACHE_ACCESS_KEY }}
          secret-access-key: ${{ secrets.NIX_CACHE_SECRET_KEY }}

      - name: Setup Nix Environment
        uses: gillouche/homelab-ci/actions/setup-nix-env@main

      - name: Inject Nexus CA into Docker Daemon
        run: |
           # Copy the baked-in cert to the shared volume visible to the DIND sidecar
           sudo mkdir -p /etc/docker/certs.d/nexus.gillouche.homelab
           sudo cp /usr/local/share/ca-certificates/nexus-ca.crt /etc/docker/certs.d/nexus.gillouche.homelab/ca.crt

      - name: Build and Push
        id: build
        run: |
          { set +x; } 2>/dev/null
          # Login to Nexus first
          echo "${{ secrets.NEXUS_PASSWORD }}" | docker login nexus.gillouche.homelab -u "${{ secrets.NEXUS_USERNAME }}" --password-stdin

          nix develop ./#default --command ./ci/build.sh "${{ inputs.image }}" "${{ inputs.version }}"
        env:
          NEXUS_REGISTRY: nexus.gillouche.homelab
          NEXUS_NAMESPACE: docker-hosted
          PUSH_IMAGES: ${{ inputs.push }}
          SCAN_IMAGES: true
          NIX_ACCESS_KEY_ID: ${{ secrets.NIX_CACHE_ACCESS_KEY }}
          NIX_SECRET_ACCESS_KEY: ${{ secrets.NIX_CACHE_SECRET_KEY }}
          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}

      - name: Export Nix Environment to PATH
        run: nix develop --command bash -c "echo \$PATH >> \$GITHUB_PATH"

      - name: Trivy Scan
        uses: gillouche/homelab-ci/actions/trivy-scan@main
        with:
          image: ${{ steps.build.outputs.scan_image }}
          trivyignore: images/${{ inputs.image }}/.trivyignore
          discord-webhook: ${{ secrets.DISCORD_WEBHOOK_SECURITY }}

      - name: Cleanup local scan image
        if: always()
        run: docker rmi "${{ steps.build.outputs.scan_image }}" 2>/dev/null || true

      - name: Push Notification
        if: steps.build.outputs.pushed == 'true' && success()
        uses: gillouche/homelab-ci/actions/push-notify@main
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK_SECURITY }}
          image: ${{ inputs.image }}
          tag: ${{ inputs.version }}
          digest: ${{ steps.build.outputs.digest }}
